# _template — copy-and-edit scaffold for a new flow

See `../../ONBOARDING.md` for the full mechanical SOP. Quick start:

    cp -r flows/_template flows/<your_flow_name>
    cd flows/<your_flow_name>
    # then edit spec.yaml and script.py; references at the bottom of
    # those files point at liberate_char/ for a real, runnable example.
