# Bolt.tcl — bolt-on tool config. Values from char_config.yaml.
set_var bolt_lib  {{ project.name }}
set_var bolt_ver  {{ project.version }}
set_var work_dir  {{ paths.work_dir }}
set_var out_dir   {{ paths.output_dir }}
