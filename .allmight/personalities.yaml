personalities:
- name: dagster-expert
  capabilities:
  - database
  - memory
  versions:
    database: 1.0.0
    memory: 1.0.0
  # Three All-Might v3 source bundles merged into this v4 personality.
  # Full lineage and per-source contribution summary in
  # personalities/dagster-expert/manifest.yaml::derived_from.
  derived_from:
  - dagster-operator
  - dagster-tutor
  - dagster-librarian
- name: dagster-ap-auditor
  capabilities:
  - memory
  versions:
    memory: 1.0.0
  # Sibling personality in this project. Strict acceptance gatekeeper
  # for the Phase-1 Dagster 1.13.3 <-> AP compatibility migration.
  # Reads dagster-expert's database/learn material, does not duplicate.
  # See personalities/dagster-ap-auditor/manifest.yaml::derived_from.
  derived_from:
  - dagster-expert
